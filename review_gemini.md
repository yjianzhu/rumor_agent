# Rumor Agent 项目技术审查报告 (Review by Gemini)

## 1. 架构与技术路线评估

### 1.1 优点与可行性
*   **清晰的分层架构**：系统目前采用了类似于 FastAPI 的清晰分层结构（`CLI -> CRUD -> Database`），使得数据库操作与业务逻辑（如 LLM 分析、数据处理）解耦。
*   **合适的数据存储选型**：使用 PostgreSQL + SQLAlchemy ORM 设计不仅稳定，在 `Rumor` 表中运用 JSONB 存储可变的 `tags`, `media_files`, `source_urls` 也为未来扩展数据的灵活性打下了良好基础。
*   **LLM 作为核心处理引擎**：使用 LLM 从非结构化文本（如爬取的网页正文或 Markdown）中抽取结构化辟谣字段（`StructuredRumorAnalysis`），在技术路线上是非常先进且完全可行的，相较于传统的 NLP/正则解析，具有极高的灵活性。
*   **配置管理与抽象**：通过 LiteLLM 和 Google ADK 对底层 LLM API 进行了封装统一（`src/llm/client.py`），能非常方便地切换不同厂商的模型。

---

## 2. 潜在瓶颈与架构风险

### 2.1 性能与并发瓶颈 (核心重灾区)
*   **单线程同步处理**：目前在 `main.py` 的主循环以及未来的扒取逻辑中，LLM 分析调用是**同步且阻塞**的。大语言模型推理耗时较长（可能几秒到十几秒），如果进行批量化导入或实时爬虫采集，同步调用将完全锁死程序的执行。
    *   *建议*：采用 `asyncio` 进行异步并发调用，或引入 Celery / RQ 这样的轻量级任务队列来进行异步的 LLM 分析任务。
*   **逐行提交数据库事务**：在 `import_jsonl_file` 中的每行记录处理都会触发 `db.commit()`。对于大规模 JSONL 文件，频繁的 IO 通信及锁竞争将带来严重性能瓶颈。
    *   *建议*：改为在一定批次（比如每 100 条）执行一次 `db.commit()`。

### 2.2 去重与标识生成方案脆弱 (Deduplication)
*   **过强的哈希约束**：`main.py` 中的 `_resolve_slug_md` 通过对 `raw_text` 全文进行 `hash_suffix` 来防止重复分析和入库。然而，哪怕 `raw_text` 仅仅多了一个空格或是爬虫抓取了一些无关广告字符，这套哈希去重就会直接失效，导致大量重复入库。
    *   *建议*：
        1.  初级方案：在哈希前做非常深度的文本规范化（仅保留汉字和字母，剔除各种符号/空行）。
        2.  高级方案（推荐）：引入基于向量嵌入 (Embeddings) + pgvector 的语义去重，判断新的谣言在语义上是否已经存在。

### 2.3 错误处理与重试机制缺失
*   **LLM 网络调用脆弱**：LLM 接口的请求极其容易因为速率限制 (Rate Limits)、网络波动而失败。目前的 `AdkLlmClient.analyze` 在遇到此种情况时未具备稳健的自动重试及退避（Exponential Backoff）策略。
    *   *建议*：利用 `tenacity` 库对 LLM 请求增加自动重试逻辑。

### 2.4 上下文长度与提示词黑盒
*   **超长文本截断**：长篇幅的新闻稿件或者多图混排抓取的 HTML/MD，可能会直接突破 `gpt-4o-mini` 或其他默认选择的模型的上下文输入上限（Context Window）。目前缺乏对超长 `raw_text` 的长度预估和切分逻辑。

---

## 3. 下一步发展建议

1.  **Phase 2 (爬虫接入) 的松耦合设计**：即将开始的 Crawler 不要与目前的入库/分析逻辑同步强耦合。建议爬虫仅把非结构化数据写入缓存区（例如 SQLite 临时库或专门的 `raw_rumors` 表），再由后台 Worker 读取数据异步并行送出 LLM 分析。
2.  **重构批处理与异常捕获**：为未来的大量数据处理作准备，设计基于批量 API 或者异步化的执行流程。
3.  **升级去重策略**：重构 Slug 和哈希系统，容忍部分杂音字符，并增加标题的模糊匹配机制，拦截潜在的变体重复数据。
